/**
 * @deprecated Utiliser useAuth() depuis @/providers/auth-provider.
 * Conservé temporairement pour compatibilité pendant la migration.
 */
export { useAuth, useIsAuthenticated } from "@/providers/auth-provider";
